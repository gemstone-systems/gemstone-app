import { Text, View } from "react-native";

const AuthCallback = () => {
    return (
        <View>
            <Text>Auth callback waowgh</Text>
        </View>
    );
};

export default AuthCallback;
